import React from "react";
import TwitchPlayer from "../components/twitchPlayer";

const About = ({ match }) => {
return (
	<div>
	<h1>
		About who?
	</h1>
	<TwitchPlayer />
	</div>
);
};

export default About;
